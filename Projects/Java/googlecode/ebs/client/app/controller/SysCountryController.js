Ext.define('ebs.controller.SysCountryController',{
	extend: 'ebs.controller.BaseController',
	alias: 'controller.sysCountryController',
	requires: ['ebs.view.sys.SysCountryListView'],
	init:
		function(){
			this.control({
			});
		}
});
