package org.tju.ebs.domain.dao;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.tju.ebs.bean.OrgEmployeeDepartment;

/**
 * A data access object (DAO) providing persistence and search support for
 * OrgEmployeeDepartment entities. Transaction control of the save(), update()
 * and delete() operations can directly support Spring container-managed
 * transactions or they can be augmented to handle user-managed Spring
 * transactions. Each of these methods provides additional information for how
 * to configure it for the desired type of transaction control.
 * 
 * @see org.tju.ebs.bean.OrgEmployeeDepartment
 * @author MyEclipse Persistence Tools
 */

public class OrgEmployeeDepartmentDAO extends BaseDAO<OrgEmployeeDepartment> {
	private static final Logger log = LoggerFactory
			.getLogger(OrgEmployeeDepartmentDAO.class);

	public void save(OrgEmployeeDepartment instance) throws Exception {
		try {
			super.save(instance);
		} catch (Exception e) {
			log.debug("");
			throw e;
		}

	}
}